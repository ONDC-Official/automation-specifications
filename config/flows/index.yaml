flows:
  - type: playground
    id: Discovery of Metro Card Recharge.
    usecase: metro-card-recharge
    tags:
      - WORKBENCH
      - REPORTABLE
    description: Discovery of Metro Card Recharge.
    config:
      $ref: ./metro-card-recharge/Discovery_of_Metro_Card_Recharge_.yaml
  - type: playground
    id: Metro Card Balance - Failure
    usecase: metro-card-recharge
    tags:
      - WORKBENCH
      - REPORTABLE
    description: Discovery of Metro Card Balance (select/on_select & init_on_init
      calls are optional).
    config:
      $ref: ./metro-card-recharge/Metro_Card_Balance_-_Failure.yaml
  - type: playground
    id: Metro Card Balance - Success
    usecase: metro-card-recharge
    tags:
      - WORKBENCH
      - REPORTABLE
    description: Discovery of Metro Card Balance (select/on_select & init_on_init
      calls are optional).
    config:
      $ref: ./metro-card-recharge/Metro_Card_Balance_-_Success.yaml
