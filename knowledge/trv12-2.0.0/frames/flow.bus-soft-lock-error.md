---
id: flow.bus-soft-lock-error
kind: instance
layer: domain
status: draft
asof: trv12-2.0.0
---

# flow.bus-soft-lock-error

Declared node for `flow.bus-soft-lock-error` — the Intercity bus flow in which the seat soft lock expires before `init` and the BPP answers `on_init` with an error instead of a quote.

Grounded at: `trv12-2.0.0:flows/index.yaml#flows[Intercity(Bus)_Error_Response(Soft Locking Time)]`

Facts live in `atoms.md` under the handle `anchor.flow-bus-soft-lock-error`; body intentionally light — no prose
assertions beyond this declaration.
